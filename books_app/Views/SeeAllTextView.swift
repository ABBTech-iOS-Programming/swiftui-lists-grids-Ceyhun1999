import SwiftUI

import SwiftUI

struct SeeAllTextView: View {
    var body: some View {
        Text("See all")
            .font(.system(size: 14, weight: .bold))
            .foregroundStyle(Color.burgundy)
    }
}

#Preview {
    SeeAllTextView()
}
